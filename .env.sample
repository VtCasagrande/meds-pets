# Este é um arquivo de exemplo para as variáveis de ambiente
# Copie este arquivo para .env.local e ajuste conforme necessário

# Conexão com o MongoDB
# Formato: mongodb://[username]:[password]@[host]:[port]/[database]?authSource=[authDB]
MONGODB_URI=mongodb://usuario:senha@localhost:27017/lembrete-meds?authSource=admin&tls=false

# Variável de ambiente do Next.js
# Use 'development' para desenvolvimento local, 'production' para produção
NODE_ENV=development

# URL base da aplicação (útil para webhooks e redirecionamentos)
# Use o URL completo com protocolo (http/https)
NEXT_PUBLIC_BASE_URL=http://localhost:3000 